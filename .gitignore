# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.pythonlibs/

# Secrets — never commit
.env
.env.*

# Replit / editor internals (.replit itself is committed on purpose)
.cache/
.config/
.local/
.agents/
.claude/settings.local.json
# Claude Code user config kept in the workspace so it survives restarts (CLAUDE_CONFIG_DIR).
# Holds login credentials and session transcripts — must never be committed.
.claude-user/

# Assignment brief — not published in a public repo
docs/
